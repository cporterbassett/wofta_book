# 2026 add-ons & redos — tunes to revisit

Open items left from the Deliverable 2 scan-replacement pass (2026-06-21). Full context,
tooling, and the side-by-side comparison report are under
`notation_pipeline/reports/2026_diff_work/` and `2026_replace_diff.pdf`.

## Needs revisiting
These two were **NOT** swapped to the new 2026 scan yet — the new PDF carries a re-annotated
ending that we want to look at deliberately before adopting it:

- **Me and My Fiddle** — current scan uses a Coda with `D.C. al Coda`; the new 2026 PDF
  drops the Coda and instead marks `End with 1A`. Melody/chords otherwise the same. Decide
  which ending convention to keep, then swap (or not) and proceed to OMR/verify.
- **Road House Ramble** — current scan uses `To Coda` + coda sign; the new 2026 PDF
  re-annotates it by hand as `To final ending` / `Final Ending`. Melody/chords otherwise the
  same. Decide, then swap (or not) and proceed to OMR/verify.

## Deliberately kept original (no action needed)
- **Ross's Reel #4** — new 2026 PDF is the same setting at poorer scan quality; kept the
  existing `source_images` scan.
